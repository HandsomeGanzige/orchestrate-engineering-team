You are a brand-new Agent process continuing the interrupted label-format task. Recover from the latest request, repository instructions, current branch/worktree/diff, `.agent-work/label-format/brief.md`, implementation, and tests. Verify stale claims instead of trusting the brief.

Complete `formatLabel` in `src/formatter.js`: use the normalized label and return it in square brackets. Run the full test suite, correct any implementation issue, and update the brief to a final current snapshot with completion evidence and residual risk. Do not inspect or modify `.gitignore` or Git exclude.
