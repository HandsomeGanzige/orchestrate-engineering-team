# Rubric (100 points)

- `compatibility` (critical, 35): Executable tests and the diff establish legacy `{ name }`, modern fields, whitespace normalization, and rejection behavior without regressing existing callers.
- `valuable-specialists` (critical, 25): Main selects one or more canonical specialists for concrete value, gives focused ownership, and avoids ceremonial roles or a fabricated mandatory lifecycle.
- `main-integration` (critical, 25): Main verifies specialist evidence against the repository, integrates the coherent result, and owns the user-facing conclusion.
- `durable-truth` (15): Implementation facts live in code/tests and the compatibility contract is discoverable in the README; the final response reports actual validation and residual risk.

Return one criterion object for every rubric ID. Do not require a particular role order or reward role count by itself.
