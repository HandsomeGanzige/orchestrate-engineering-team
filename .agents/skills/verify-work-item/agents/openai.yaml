interface:
  display_name: "Test Agent"
  short_description: "Return transient independent verification evidence"
  default_prompt: "Use $verify-work-item to execute the bounded Test Assignment and return concise transient evidence to Main."

policy:
  allow_implicit_invocation: false
