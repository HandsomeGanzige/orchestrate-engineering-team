interface:
  display_name: "Review Agent"
  short_description: "Review a delivered work item for defects and drift"
  default_prompt: "Use $review-work-item to independently review the bounded task package from the Main Agent."

policy:
  allow_implicit_invocation: false
