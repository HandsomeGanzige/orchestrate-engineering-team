The project owner has approved and authorized this decision: a future `ApiClient` implementation will cache successful GET responses for 60 seconds, keyed by HTTP method plus canonical URL; mutation responses and errors must never be cached. This is confirmed project policy, not a proposal. Do not implement it in this task.

Use an independent Architecture specialist with confirmed workspace-write access to directly author exactly `docs/adr/0002-api-cache.md` and update `docs/README.md` to link it. The ADR must follow existing conventions and state status, scope, rationale, invariants, compatibility constraints, implementation/test references, and supersession. Apart from optional `.agent-work/` material, no other file may change. `AGENTS.md` is specifically out of scope. Main must inspect and integrate the documentation result.
