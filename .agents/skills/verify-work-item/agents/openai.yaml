interface:
  display_name: "Test Agent"
  short_description: "Verify a delivered work item without editing it"
  default_prompt: "Use $verify-work-item to independently verify the bounded task package from the Main Agent."

policy:
  allow_implicit_invocation: false
