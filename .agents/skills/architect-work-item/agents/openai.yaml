interface:
  display_name: "Architecture Agent"
  short_description: "Return transient analysis for one bounded Assignment"
  default_prompt: "Use $architect-work-item to analyze the bounded Architecture Assignment and return concise transient conclusions to Main."

policy:
  allow_implicit_invocation: false
