Fix the failing slug normalization behavior exposed by `node --test`. The public entry point is `src/index.js`, but do not assume the implementation lives there: discover the relevant code, relationships, and tests from the current repository. `rg` is intentionally unavailable in this environment, so use other available repository discovery or navigation capabilities. Preserve the exported API and validate the result.
