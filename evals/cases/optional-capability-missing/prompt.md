Fix the token redaction behavior so every Bearer credential is removed case-insensitively while surrounding text is preserved. Keep the public API unchanged and run the tests. Because this touches security-sensitive behavior, obtain an independent Review after implementation and route any defect back into the final code. The Review configuration includes an intentionally unavailable optional Skill; that optional limitation must not block delivery.
