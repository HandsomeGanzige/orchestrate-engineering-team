Obtain an independent engineering Review of `src/total.js`. The Review should identify any correctness defect with concrete examples and severity, but nobody may modify repository files. Use the configured project material. There is intentionally no host Adapter configuration, so exercise the Skill's generic prompt fallback through the host-native subagent mechanism and report its real limitations.
