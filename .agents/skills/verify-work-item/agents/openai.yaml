interface:
  display_name: "Test Agent"
  short_description: "Execute a bounded independent Test Assignment"
  default_prompt: "Use $verify-work-item to execute the bounded Test Assignment from the Main Agent."

policy:
  allow_implicit_invocation: false
